--- a/github.com/slackhq/nebula/udp/conn.go	2026-04-16 09:47:08
+++ b/github.com/slackhq/nebula/udp/conn.go	2026-04-16 09:47:07
@@ -18,6 +18,9 @@
 	LocalAddr() (netip.AddrPort, error)
 	ListenOut(r EncReader)
 	WriteTo(b []byte, addr netip.AddrPort) error
+	// Flush sends any packets queued by WriteTo in a batch syscall.
+	// Platforms without batch send support MUST implement this as a no-op.
+	Flush() error
 	ReloadConfig(c *config.C)
 	SupportsMultipleReaders() bool
 	Close() error
@@ -40,6 +43,9 @@
 func (NoopConn) WriteTo(_ []byte, _ netip.AddrPort) error {
 	return nil
 }
+func (NoopConn) Flush() error {
+	return nil
+}
 func (NoopConn) ReloadConfig(_ *config.C) {
 	return
 }
--- a/github.com/slackhq/nebula/udp/udp_linux.go	2026-04-16 09:47:08
+++ b/github.com/slackhq/nebula/udp/udp_linux.go	2026-04-16 09:47:07
@@ -250,6 +250,10 @@
 
 		return nil
 	}
+}
+
+func (u *StdConn) Flush() error {
+	return nil
 }
 
 func (u *StdConn) ReloadConfig(c *config.C) {
--- a/github.com/slackhq/nebula/udp/udp_generic.go	2026-04-16 09:47:08
+++ b/github.com/slackhq/nebula/udp/udp_generic.go	2026-04-16 09:47:07
@@ -44,6 +44,10 @@
 	return err
 }
 
+func (u *GenericConn) Flush() error {
+	return nil
+}
+
 func (u *GenericConn) LocalAddr() (netip.AddrPort, error) {
 	a := u.UDPConn.LocalAddr()
 
--- a/github.com/slackhq/nebula/udp/udp_tester.go	2026-04-16 09:47:08
+++ b/github.com/slackhq/nebula/udp/udp_tester.go	2026-04-16 09:47:07
@@ -106,6 +106,10 @@
 	return nil
 }
 
+func (u *TesterConn) Flush() error {
+	return nil
+}
+
 func (u *TesterConn) ListenOut(r EncReader) {
 	for {
 		p, ok := <-u.RxPackets
--- a/github.com/slackhq/nebula/udp/udp_rio_windows.go	2026-04-16 09:47:08
+++ b/github.com/slackhq/nebula/udp/udp_rio_windows.go	2026-04-16 09:47:07
@@ -335,6 +335,8 @@
 func (u *RIOConn) Rebind() error {
 	return nil
 }
+
+func (u *RIOConn) Flush() error { return nil }
 
 func (u *RIOConn) ReloadConfig(*config.C) {}
 
